{
  "name": "dnd-2024-reference",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "D&D 2024 rules glossary, weapons and weapon masteries reference, built from SRD 5.2.1",
  "scripts": {
    "dev": "astro dev",
    "validate": "node scripts/validate_glossary.mjs && node scripts/validate_weapons.mjs && node scripts/validate_equipment.mjs",
    "build": "npm run validate && astro check && astro build && node scripts/generate-sw.mjs",
    "preview": "astro preview",
    "pretest": "npm run build",
    "test": "playwright test",
    "test:update": "npm run build && playwright test --update-snapshots",
    "check": "astro check"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource/cinzel": "^5.3.0",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.13.5",
    "typescript": "^6.0.3"
  }
}
